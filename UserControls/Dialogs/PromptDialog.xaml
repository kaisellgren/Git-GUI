<Window x:Class="GG.UserControls.Dialogs.PromptDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        mc:Ignorable="d"
        Icon="../../Resources/Icons/Git-GUI.ico"
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterScreen">
    
    <Window.Background>
        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
            <GradientStop Color="#fff" Offset="0" />
            <GradientStop Color="#f9f9f9" Offset="0.05" />
            <GradientStop Color="#e9e9e9" Offset="0.95" />
            <GradientStop Color="#f9f9f9" Offset="1" />
        </LinearGradientBrush>
    </Window.Background>
    
    <StackPanel Margin="32" HorizontalAlignment="Left">
        <TextBlock x:Name="MessageBlock" Text="You have not entered a message." Margin="0,0,0,8" />
        <TextBox x:Name="ResponseTextBox" Margin="0,0,0,8" Width="256" />
        <Button Content="OK" Click="OnOK" HorizontalAlignment="Right" Padding="16,2" />
    </StackPanel>
</Window>