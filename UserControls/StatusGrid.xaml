<UserControl x:Class="GG.UserControls.StatusGrid"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:converters="clr-namespace:GG.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="343" d:DesignWidth="501">
    <UserControl.Resources>
        <converters:StatusToColorConverter x:Key="statusToColorConverter" />
        <converters:StatusToCharacterConverter x:Key="statusToCharacterConverter" />
        <converters:ExtensionToImageConverter x:Key="extensionToImageConverter" />
    </UserControl.Resources>
    <DockPanel>
        <DataGrid SelectionUnit="FullRow" HeadersVisibility="Column" ItemsSource="{Binding StatusItemsGrouped}" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTemplateColumn Header="St">
                    <DataGridTemplateColumn.CellTemplate>
                        <ItemContainerTemplate>
                            <WrapPanel HorizontalAlignment="Center">
                                <WrapPanel.Effect>
                                    <DropShadowEffect BlurRadius="3" Color="Black" ShadowDepth="0" Direction="315" />
                                </WrapPanel.Effect>
                                <Border BorderThickness="1" CornerRadius="4" Background="{Binding Status, Converter={StaticResource statusToColorConverter}}">
                                    <Border.BorderBrush>
                                        <SolidColorBrush>
                                            <SolidColorBrush.Color>
                                                <Color A="96" R="0" G="0" B="0" />
                                            </SolidColorBrush.Color>
                                        </SolidColorBrush>
                                    </Border.BorderBrush>
                                    <TextBlock
                                        Width="16"
                                        VerticalAlignment="Stretch"
                                        HorizontalAlignment="Center"
                                        TextAlignment="Center"
                                        Foreground="White"
                                        FontFamily="Arial"
                                        FontSize="11"
                                        FontWeight="Bold"
                                        TextOptions.TextFormattingMode="Display"
                                        TextOptions.TextRenderingMode="ClearType"
                                        Text="{Binding Status, Converter={StaticResource statusToCharacterConverter}}" />
                                </Border>
                            </WrapPanel>
                        </ItemContainerTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                
                <DataGridTemplateColumn Header="Filename" Width="*">
                    <DataGridTemplateColumn.CellTemplate>
                        <ItemContainerTemplate>
                            <WrapPanel>
                                <Image Source="{Binding Extension, Converter={StaticResource extensionToImageConverter}}" Width="16" Margin="0,0,2,0" />
                                <TextBlock Text="{Binding Path=Filename}" />
                            </WrapPanel>
                        </ItemContainerTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                
                <DataGridTextColumn Binding="{Binding Path=Type}" Header="Type"></DataGridTextColumn>
                <DataGridTextColumn Binding="{Binding Path=Size}" Header="Size (kB)"></DataGridTextColumn>
                <DataGridTextColumn Binding="{Binding Path=Size}" Header="Binary"></DataGridTextColumn>
            </DataGrid.Columns>

            <DataGrid.GroupStyle>
                <GroupStyle>
                    <GroupStyle.HeaderTemplate>
                        <DataTemplate>
                            <StackPanel>
                                <TextBlock Text="{Binding Name}" />
                            </StackPanel>
                        </DataTemplate>
                    </GroupStyle.HeaderTemplate>
                    <GroupStyle.ContainerStyle>
                        <Style TargetType="{x:Type GroupItem}">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="{x:Type GroupItem}">
                                        <StackPanel>
                                            <Border BorderBrush="#aaa" BorderThickness="0,1,0,1">
                                                <Grid Style="{DynamicResource DataGridGroupItemStyle}">
                                                    <StackPanel Orientation="Horizontal" Margin="4,3,0,3">
                                                        <TextBlock Text="{Binding Name}" />
                                                        <TextBlock xml:space="preserve">: </TextBlock>
                                                        <TextBlock Text="{Binding ItemCount}"/>
                                                    </StackPanel>
                                                </Grid>
                                            </Border>
                                            <ItemsPresenter />
                                        </StackPanel>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </GroupStyle.ContainerStyle>
                </GroupStyle>
            </DataGrid.GroupStyle>
        </DataGrid>
    </DockPanel>
</UserControl>
